services:
  app:
    build: .
    ports:
      - "18000:80"
    environment:
      - SECRET_KEY=${SECRET_KEY:-prod_super_secret_key_12345}
      - ENV=production
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always

